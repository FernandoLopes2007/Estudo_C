#include <stdio.h>

int main(){
    int n, fatorial;
    do{
        printf("Digite um numero inteiro maior ou igual a zero:\n--> ");
        scanf("%d", &n);
    }
    while(n <=0);
    for(int i= n-1; i>0; i--){
        n = n * i;
    }
    printf("%d", n);
    return 0;
}